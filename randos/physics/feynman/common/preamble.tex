% !TEX root = ../feynman.tex
% This line tries to make sure proper root file is built by VSCode. It
% has some kind of autodetect logic for the rootfile which can fail.

\documentclass[11pt, oneside]{amsart}

\usepackage{geometry}
\geometry{letterpaper}

\usepackage{ned-common}
\usepackage{ned-calculus}
\usepackage{ned-linear-algebra}
\usepackage{ned-stats}

\usepackage[version=4]{mhchem}
\usepackage[free-standing-units=true]{siunitx}

% newcommands used across sections
% TODO: should these move out to my common libs?

\newcommand{\CoM}{\text{CoM}}
